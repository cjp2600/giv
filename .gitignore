# Build output (do not commit the binary)
/giv

# IDE
.idea/

# Secrets / env templates (keep local only)
.env
.env.example

# Maintainer-only markdown under packaging/ (keep locally; avoid accidental commits)
/packaging/RELEASE.md
/packaging/homebrew/README.md

# Local automation (PAT push, brew formula sync — keep copies only on your machine)
/scripts/
